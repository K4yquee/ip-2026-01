package main

import "fmt"

func main() {
	const n int = 10
	var val [n]float64

	for i := 0; i < n; i++ {
		fmt.Printf("Digite o valor N%d ", i)
		fmt.Scan(&val[i])
	}

	fmt.Println("Valores em ordem inversa:")
	for i := n - 1; i >= 0; i-- {
		fmt.Println(val[i])
	}
}
